<nav class="navbar navbar-dark navbar-expand-md bg-dark sticky-top justify-content-md-center justify-content-start">
    <button class="navbar-toggler ml-1" type="button" data-toggle="collapse" data-target="#collapsingNavbar2">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="navbar-collapse collapse justify-content-between align-items-center w-100" id="collapsingNavbar2">
        <ul class="navbar-nav mx-auto text-md-center text-left">
            <li class="nav-item my-auto">
				<%= link_to "Articles", articles_path, class: "nav-link" %>
            </li>
            <li class="navbar-brand my-auto mx-5">
                <%= link_to image_tag('logo.png', class: 'img'), root_path, class: "navbar-brand d-inline", id: "logo" %> 
            </li>
            <li class="nav-item my-auto">
                <%= link_to "About", about_path, class: "nav-link" %>
            </li>
        </ul>
		<ul class="nav navbar-nav navbar-dark bg-dark flex-row justify-content-md-center justify-content-start flex-nowrap">
			<li class="nav-item my-auto ml-auto">
				<% if logged_in? %>
					<%= link_to "LOGOUT", logout_path, class: "nav-link", id: "singup", method: :delete %>
				<% else %>
					<%= link_to "SINGIN", login_path, class: "nav-link", id: "singup" %>
				<% end %>
        </ul>
    </div>
	<div class="blur"></div>
</nav>

